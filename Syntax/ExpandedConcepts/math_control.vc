init count = 5

make main() {
    while count > 0 {
        out: count
        count = count - 1
    }

    check count == 0 {
        out: "Done"
    } else {
        out: "Something went wrong"
    }

    return
}
